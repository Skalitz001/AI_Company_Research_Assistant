# Required provider credentials (keep values server-side; never commit .env).
SERPER_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_DEFAULT_MODEL=nvidia/nemotron-3-super-120b-a12b:free

# Optional configuration. Empty values use the application defaults.
OPENROUTER_MODEL_SUGGESTIONS=
OPENROUTER_APP_URL=
CRAWLER_USER_AGENT=
DISCORD_ENABLED=false
PORT=
